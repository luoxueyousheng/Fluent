{
  "name": "fluent-react",
  "private": true,
  "version": "0.1.0",
  "description": "Fluent 2 / WinUI 3 风格 React 组件库(JadeView 宿主,Windows)",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/template",
    "build": "npm run build -w apps/template",
    "build:lib": "npm run build -w @fluent-react/ui && npm run build -w @fluent-react/bridge",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "puppeteer-core": "^25.3.0",
    "typescript": "^5.7.0"
  }
}
